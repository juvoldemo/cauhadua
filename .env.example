# Connect Postgres (for example Neon) and set DATABASE_URL in Vercel.
# Use the provider's pooled connection URL with SSL enabled.
# Never commit real credentials or put them in public/.
DATABASE_URL=
# A non-empty private admin password of any length. Set this in Vercel too.
ADMIN_PASSWORD=1987
